name: jarvis
description: Jarvis — local assistant scaffold
publish_to: "none"
version: 0.1.0
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.0.0
  dio: ^5.0.0
  isar: ^3.0.0
  path_provider: ^2.0.0
  permission_handler: ^10.0.0
  speech_to_text: ^5.6.0
  flutter_tts: ^3.5.0
  shared_preferences: ^2.0.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.0
  isar_generator: ^2.0.0
  lint: ^2.0.0
